import React from "react";

const StyleWrap = ({ outputArea, inputArea }) => (
  <div className="container">
    <div className="outputArea">{outputArea}</div>
    <div className="inputArea">{inputArea}</div>
  </div>
);

export default StyleWrap;
